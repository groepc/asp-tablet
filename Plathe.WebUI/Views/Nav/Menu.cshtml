@model IEnumerable<string>

@Html.ActionLink("Genre", null,
    new { @class = "btn btn-CodeBlockType btn-default btn-lg" })

@foreach (var link in Model)
{
    @Html.RouteLink(link, new object
    {
                
    }, new
    {
        @class = "btn btn-block btn-default btn-lg"
    })
}
